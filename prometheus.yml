global:
  scrape_interval: 5s # 每5秒抓取一次数据，观察更实时
  evaluation_interval: 5s

scrape_configs:
  - job_name: "go-ai-gateway"
    static_configs:
      - targets: ["172.19.0.1:8080"] # 使用 docker-compose 中的服务名
  # add this
  - job_name: "gpu"
    static_configs:
      - targets: ["172.17.141.222:9835"]